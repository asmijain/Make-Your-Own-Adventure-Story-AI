# Make Your Own Adventure Story AI

An AI-powered "choose your own adventure" story generator built with FastAPI and React. Users provide a theme, and the app generates a branching narrative tree using an LLM, then lets the user navigate the story by choosing between options at each step until they reach an ending.

## Live Demo

URL - https://adventure-story-ai-beta.vercel.app/

## Features

- **AI-generated branching stories** — give a theme, get a unique "choose your own adventure" narrative tree generated by an LLM (via Groq through LangChain)
- **User authentication** — register/login with email + password, JWT-based session handling (`python-jose`, bcrypt password hashing via `passlib`)
- **Story persistence** — generated stories are saved to the database and tied to the session that created them, so completed stories can be revisited
- **Async generation with job polling** — story creation kicks off a background job; the frontend polls job status until the story is ready, instead of blocking on one long request

## Project Structure

```
.
├── backend/
│   ├── main.py                  # FastAPI app instance, CORS config, router registration
│   ├── db/
│   │   └── database.py          # SQLAlchemy engine, session, Base, create_tables()
│   ├── models/                  # SQLAlchemy ORM models
│   │   ├── story.py              # Story, StoryNode
│   │   ├── job.py                 # StoryJob (tracks async generation status)
│   │   └── user.py                 # User (auth)
│   ├── schemas/                 # Pydantic request/response schemas
│   │   ├── story.py
│   │   ├── job.py
│   │   └── auth.py                # Register/Login/Token/UserResponse schemas
│   ├── routers/
│   │   ├── story.py               # POST /stories/create, GET /stories/{id}/complete
│   │   ├── job.py                  # GET /jobs/{job_id} — poll generation status
│   │   └── auth.py                  # POST /auth/register, POST /auth/login, GET /auth/me
│   ├── core/
│   │   ├── config.py               # Settings (env vars): DB, CORS, GROQ key, JWT config
│   │   ├── security.py              # password hashing/verification, JWT encode/decode
│   │   ├── deps.py                   # get_current_user dependency (JWT auth guard)
│   │   ├── prompts.py                 # prompt templates for the LLM
│   │   └── story_generator.py          # prompt construction + LLM call + response parsing
│   └── requirements.txt
├── frontend/
│   ├── src/
│   │   ├── components/             # StoryGenerator, StoryGame, StoryLoader, ThemeInput,
│   │   │                            # LoadingStatus, Login, Register, Navbar
│   │   ├── context/
│   │   │   └── AuthContext.jsx       # auth state (current user, token) shared across the app
│   │   ├── api/
│   │   │   └── auth.js                # fetch wrappers for register/login/me
│   │   ├── util.js
│   │   └── App.jsx
│   ├── package.json
│   └── vite.config.js
└── README.md
```

## Tech Stack

**Backend**
- **Python 3**
- **FastAPI** — routing, request validation, background task execution
- **SQLAlchemy** — ORM mapping Python classes to database tables (`Story`, `StoryNode`, `StoryJob`, `User`)
- **Pydantic / pydantic-settings** — request/response validation and environment-based config
- **python-jose** — JWT creation and verification for authenticated sessions
- **passlib[bcrypt]** — password hashing
- **LangChain + langchain-groq** — orchestrates prompting against Groq's LLM API to generate the branching story content
- **SQLite** for local development / **PostgreSQL** in production on Render
- **Uvicorn** — ASGI server running the FastAPI app
- 
**Frontend**
- **React** — component-based UI
- **JavaScript**
- **Fetch API** — communicates with the backend (auth, story creation, polling job status, fetching the completed story tree)
- **React Context** — shares auth state (current user, token) across components
- **CSS** — component-scoped and global styling
- 
**Infrastructure / DevOps**
- **Vercel** — hosts and auto-deploys the frontend on push
- **Render** — hosts and auto-deploys the backend web service on push, also runs the PostgreSQL database in production

### Backend

```bash
cd backend
python -m venv venv
source venv\Scripts\activate        # Windows
# source venv/bin/activate   # macOS/Linux
pip install -r requirements.txt
```

Create a `.env` file inside `backend/`:

```
GROQ_API_KEY=your_key_here
DATABASE_URL=sqlite:///./stories.db
JWT_SECRET_KEY=your_long_random_secret_here   # generate with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
```
Start the server:

```bash
uvicorn main:app --reload
```

API docs (Swagger UI) will be available at `http://localhost:8000/docs`.

### Frontend

```bash
cd frontend
npm install
```

Create a `.env` file inside `frontend/`:

```
VITE_API_BASE_URL=http://localhost:8000
```

Start the dev server:

```bash
npm run dev
```

## API Overview

| Method | Endpoint | Auth required | Description |
|---|---|---|---|
| POST | `/api/auth/register` | No | Create a new user account, returns a JWT |
| POST | `/api/auth/login` | No | Log in with email + password, returns a JWT |
| GET | `/api/auth/me` | Yes | Get the current authenticated user |
| POST | `/api/stories/create` | No | Kick off async story generation for a given theme, returns a job ID |
| GET | `/api/jobs/{job_id}` | No | Poll the status of a story generation job |
| GET | `/api/stories/{story_id}/complete` | No | Fetch the full generated story tree once ready |
